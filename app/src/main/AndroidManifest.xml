<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doctoracho">


    <uses-permission android:name="android.permission.INTERNET"></uses-permission>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gastroenterologyActivity"></activity>
        <activity android:name=".anestheticsActivity" />
        <activity android:name=".cardiologyActivity" />
        <activity android:name=".neurologyActivity" />
        <activity android:name=".RegisterDoctor" />
        <activity android:name=".LoginAdmin" />
        <activity android:name=".LoginDoctor" />
        <activity android:name=".RegShowActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>